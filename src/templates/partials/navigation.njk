<!-- Navigation panel -->
<nav class="main-nav dark transparent stick-fixed">
    <div class="full-wrapper relative clearfix">
        <!-- Logo ( * your text or image into link tag *) -->
        <div class="nav-logo-wrap local-scroll">
            <a href="#top" class="logo">
            {% if site.logo %}
                <img src="{{ site.logo }}" alt="" />
            {% endif %}
            </a>
        </div>
        <div class="mobile-nav ">
            <i class="fa fa-bars"></i>
        </div>
        <!-- Main Menu -->
        <div class="inner-nav desktop-nav">
            <ul class="clearlist scroll-nav local-scroll">
                <!-- Item With Sub -->
                <li>
                    <a href="#" class="mn-has-sub">Art <i class="fa fa-angle-down"></i></a>

                    <!-- Sub -->
                    <ul class="mn-sub" >
                        {% for project in site.projects %}
                          {% if project.type == "art" %}
                            <li><a href="#{{ project.id }}" data-owl-slider="#home" data-owl-slider-index="{{ loop.index0 }}" class="slider-btn" >{{ project.title }}</a></li>
                          {% endif %}
                        {% endfor %}
                    </ul>
                    <!-- End Sub -->

                </li>
                <!-- End Item With Sub -->

                <!-- Item With Sub -->
                <li>
                    <a href="#" class="mn-has-sub">Tech <i class="fa fa-angle-down"></i></a>

                    <!-- Sub -->
                    <ul class="mn-sub" >
                        {% for project in site.projects %}
                          {% if project.type == "tech" %}
                            <li><a href="#{{ project.id }}" data-owl-slider="#home" data-owl-slider-index="{{ loop.index0 }}" class="slider-btn">{{ project.title }}</a></li>
                          {% endif %}
                        {% endfor %}
                    </ul>
                    <!-- End Sub -->

                </li>
                <!-- End Item With Sub -->
                <li><a href="#about">About</a></li>
            </ul>
        </div>
    </div>
</nav>
<!-- End Navigation panel -->
